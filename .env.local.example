DATABASE_URL=file:./data/nextleap.db

# Orchestration agent (lib/agent/orchestrator.ts) — needed to actually run createAgent().stream()
GROQ_API_KEY=

# OSM MCP tool (lib/agent/tools/osmNearby.ts) — SSE bridge URL, pending per docs/ARCHITECTURE.md §8
OSM_MCP_URL=

# Booking MCP tools (lib/agent/tools/*BookingHold.ts, listBookingSlots.ts)
BOOKING_MCP_URL=
BOOKING_MCP_API_KEY=

# n8n shortlist-to-PDF-email workflow (app/api/notify/route.ts) — see n8n/README.md
N8N_WEBHOOK_URL=
